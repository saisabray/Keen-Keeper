import React from 'react';

const FriendDetailsLayout = ({children}) => {
    return (
      <div className="min-h-screen bg-gray-100 text-white">{children}</div>
    );
};

export default FriendDetailsLayout;
